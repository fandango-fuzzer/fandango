version: "3.9"

services:
  postfix:
    build: ./image
    container_name: postfix_coverage
    ports:
      - "8025:25"           # expose SMTP locally
    volumes:
      - ./coverage_snapshots:/coverage   # coverage logs
    tty: true